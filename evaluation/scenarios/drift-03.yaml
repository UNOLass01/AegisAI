id: drift-03
problem: "False positives have spiked since the v2 rollout. What changed?"
injected_condition:
  type: model_state
  focus: v2
expected_root_cause:
  key: data_drift
  keywords: [drift, transaction_amount, false positive, precision]
expected_tools_used: [get_metrics, analyze_logs, analyze_model, search_knowledge]
notes: "Symptom-led query anchored on the v2 rollout; a version-less agent cannot resolve this against the latest state."
